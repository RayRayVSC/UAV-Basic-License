# 0.0.2.0 投影片網頁：給 agent 與產生器的規則

本資料夾的網頁由 `build.py` 從 `pages/*.md` 產生。動手前先讀完這份規則；尺寸與字級的原始依據是 `../0.0.0.2/投影片規格表.md`。

## 1. 工作流程

1. 每一頁的內容與版型先和作者討論，定案後才寫內容檔。細節（字級、圖片位置、表格樣式）不自行決定。
2. 只改 `pages/*.md`、`_template/`、元件 CSS。**不要手改產生出來的 HTML**，下次 build 會覆蓋。
3. 改完跑 `python build.py`，看 `build/report.md` 與 `build/shots/` 的截圖，全部 PASS 才算完成。
4. 共用元素（頁首帶、頁尾、翻頁按鈕、腳本）只改 `_template/slide.template.html`，改完重跑 build 讓所有頁同步。
5. 新元件：先寫 CSS 檔，再到 `build.py` 的 `TYPES` 登記 type，最後才能在內容檔使用。
6. 每段知識教學附上題庫考題、課堂互動練習：每完成一個小章節，該節最後一頁的「下一頁」連到隨堂考頁（type: quiz）。隨堂考檔名就是它服務的教材章節 `ch{章}_Quiz_{節}`，產生器自動把它接在該節最後一頁之後（節只有一層如 `2` 時接在 2.x 最後一頁之後）；沒有對應教學頁的隨堂考自成一條序列。題目取自 `bank/data_pro.js`，每次 5 題，題池由作者確認。

## 2. 固定不動的部分

- 舞台 1280 × 720，深色主題，雷達背景與掃描線。
- 頁首帶：「無人機 ／ 專業基本級操作證」，y 20–58。
- 頁尾帶：署名「台中市政府輔導　台中無人機創造發展基地－無人機教材 專業基本級操作證」，y 682–720，置中。
- 翻頁按鈕：左「上一頁」遙控器圖示、右「下一頁」穿越機圖示，含動畫；鍵盤 ← → 與 PageUp／PageDown 翻頁。連結由產生器依頁序填入，第一頁的上一頁與最後一頁的下一頁為 `#`。
- 這些都在範本裡，內容檔不碰。

## 3. 版面尺寸

| 區塊 | 位置 | 上限 |
|---|---|---|
| 標題框（主標、分隔號、副標、引言） | x 112，y 67–211 | 內容高 144 px |
| 內容框 `.slide-content` | x 112，寬 1056，y 221–680 | 正文、圖、附註限 y 221–642，內容高 421 px；法規行另佔 y 642–680 |
| 頁尾帶 | y 682–720 | 固定 |
| 翻頁按鈕 | 左右各約 112 px 寬，y 642–714 | 會伸進內容框底部。y 642 到 680 這一條**只能放法規一行**（`p.tip`，CSS 固定貼在內容框最底 38 px、垂直置中、左右內縮 130 px，不佔內容流；不顯示「TIP｜相關法規」字樣，條號用 `<b class="law">§28</b>` 橘色數字字型）；正文、圖、圖說、附註（※）都必須在 y 642 以上 |

- 字型、色票、表格列高（4 的倍數）、格線規格依投影片規格表 §3 到 §6。
- 正文內容超過 421 px 就分頁，不縮字、不改框。
- 附註（※ 資料來源、查證日期等）若要放在頁底，改寫成法規行（p.tip）才能進 y 642 以下；否則放在 642 以上。

## 4. 檔名規則

`ch{章}p{頁}_{章節}_{短語意}[_{其他}].html`，內容檔同名、副檔名 `.md`。

| 段 | 說明 | 例 |
|---|---|---|
| ch | 教材章：0 前置頁（封面、講師介紹）、1 操作證、2 學科、3 術科 | ch2 |
| p | 章內流水號，兩位數 | p33 |
| 章節 | 第二章用《遙控無人機學科測驗指南》編號（見本資料夾 `學科測驗指南_章節標題_1.1-5.3.md`，2.1 到 5.3；前言 1.3 到 1.6 不做教學頁，其隨堂考給第一章 1.2 用）；第一、三章用《無人機教材編寫筆記_修改版.md》的「節.小節」。第二章另有 1.0 報名與考試規則、3.0 三種構造兩頁前置節。第一章：1.0 課程主線、1.1 為什麼要考照、1.2 什麼是無人機、1.3 什麼是無人機操作證、1.4 操作限制排除、2.1 誰在管、2.2 管理資訊系統、2.3 天空能不能飛、2.4 地面能不能操作、3.1 被管哪裡 操作人、4.1 法人作業流程、4.2 考照判斷核心。第三章（主標：通過術科的三個因素／術科測驗項目／考場；無隨堂考，題庫只有學科）：1.1 總覽、1.2 天份、1.3 努力、1.4 運氣、2.1 送分項目流程、2.2 性能諸元（第二頁：考試機規格與操作模式）、2.3 A 飛行前檢查（第二頁：360 度檢查表）、2.4 B 定點起降及四面停懸、2.5 C 8 字水平圓、2.6 D 側面懸停及前進後退、2.7 E 高度保持五邊飛行、2.8 F 緊急處置程序、2.9 G 飛行後檢查、3.1 考場機率、3.2 考試上電、3.3 考場緊急處置、3.4 五邊飛行風向 | 3.1、1.3、2.4 |
| 短語意 | 英文，Title_Case，底線相連；指南有英文的沿用（Lift、Drag、Load_Factor） | Newtons_Laws |
| 其他 | 選填：同一節拆頁 `_2`、表格頁 `_Table` 等 | `_2` |

- 頁序由（章, 頁）決定，插頁時改該章後面頁的 p 即可，改名就是重新命名 md 檔。檔頭 `prev:`／`next:` 可覆寫產生器算出的連結；目前第三章最後一頁 3.4 的下一頁接到術科動畫模擬器。
- custom 頁的資產檔（css、js、README、檢查腳本）用 `ch{章}_{章節}_` 前綴，不含 p，避免插頁時連動改名。
- 隨堂考頁：`ch{章}_Quiz_{節}.html`，節號＝它服務的教材章節；合併節用範圍 `ch2_Quiz_3.5-3.6.html`，整章綜合 `ch2_Quiz_3.html`。沒有頁碼，由產生器依章節號接進教學序列，未匹配者依章節順序自成一條序列。第二章的節號＝學科指南節號，題號對照存在 `bank/sections.json`。

## 5. 內容檔格式

```
---
title: 分頁標題（可省略，預設「主標－副標｜投影片版（固定 1280×720）」）
description: meta 描述（可省略，預設用引言）
main: 主標
sub: 副標
lead: 引言一句，模組間用「　；　」分隔
type: matrix | levels | custom
css: 額外要載入的 CSS，逗號分隔（可省略）
file: custom 頁的 HTML 檔名（僅 custom）
foot_link: 頁尾按鈕「標籤 | 連結」（可省略）
prev: / next: 覆寫前後頁連結（可省略；例如最後一頁 next 接到 ../data/Examination-Simulator/Examination.html）
---
內容框的 HTML 片段
```

- 標題三段的字級固定：主標 48 pt、副標 36 pt、引言 1.05 rem；引言超過一行就縮短，不換行。
- 副標 `sub` 必須以章節序號開頭，格式 x.y，與檔名的章節段一致：`1.3 什麼是無人機操作證`；合併節寫 `3.5-3.6` 或 `3.5 到 3.6`；整章綜合寫 `第 3 章 …`。build 會檢查，不符者 FAIL（blank 頁除外）。
- 內容框的正文直接寫 HTML；純文字頁的 md 自動轉換先不做，保留選項。
- 圖片一律放 `img/`（單一資料夾，不分章），檔名 `ch{章}_` 前綴加英文小寫底線，例如 `img/ch1_case_a_ccK_airport.png`。
- 文加圖版型：左文右圖（`.fig-row`），圖片**高度優先**：最高 390 px、寬度依比例、最寬 560 px（`.narrow` 340 px），右欄寬＝圖寬，文字吃剩下的寬度；單圖頁（`.full`）最高 390 px；並排圖列（`.fig-strip`）最高 300 px。正文字級維持 18 pt，版面空白用放大圖片來填，不放大文字；已確認。
- 案例頁一個案例一頁（`.fig-row`，左標題與要點、右大圖），不併頁。
- 圖高微調：`figure.fig` 的 `--fh`（預設 390 px），頁面可寫 `style="--fh:260px"`；橫幅特寬的圖用 `.fig-row.big`（最寬 700 px）；步驟文字多的科目頁用 `.fig-row.mid`（圖最寬 480 px）搭配 `.txt.compact`。
- 外部連結按鈕：放在頁尾「下一頁」左邊（`.foot-link`，y 680–710、右緣 x 1038），內容檔檔頭寫 `foot_link: ▶ 標籤 | ../data/…`，產生器填進範本插槽 `{{foot_link}}`，開新分頁；標籤要短（約 8 個字），避免碰到置中的署名。相對路徑從 `0.0.2.0/` 起算。正文內若要放連結按鈕，用 `<p class="links"><a class="btn-link" …>`。
- 影片：不用 YouTube 內嵌，影片檔放 `video/`（`ch{章}_` 前綴），用 ffmpeg 轉成 1280×720 H.264 + AAC（`-crf 25 -movflags +faststart`），另抽一張 poster 圖放 `img/`；頁面用 `<video controls preload="metadata" poster=… width=… height=…>`，寫在 `figure.fig` 內，尺寸規則同圖片。原始大檔留在 `data/`。
- quiz 頁不寫正文，檔頭 `questions:` 列整個題池的題號（該小節範圍內的全部題目，由作者確認），`show: 5` 為一次顯示題數；可選 `explain_Q102: 一句解說`、`bank:`、`bank_var:`。build 時把題池嵌進頁面，量測與截圖用題池前 5 題（固定模式），開頁時由 quiz.js 隨機抽。build 會量整個題池每張卡的高度，超過 150 px 的題號會在報告裡提醒，抽到放不下時頁面會自動重抽。

## 6. 可用的 type

| type | 元件 CSS | 附加腳本 | 用途 |
|---|---|---|---|
| matrix | matrix.css | 無（app.js 內建同色連動） | 資格矩陣表 |
| levels | levels.css | `_template/type-levels.script.html` 同色連動 | 級別表、八款對照表、卡片頁 |
| page | page.css | 無 | 通用版型：正文（.txt）、文＋圖（.fig-row）、方塊（.grid2/.grid3/.grid5 + .box）、流程（.flow）、步驟（.steps）、引句（.quote）、規格表（table.spec）、附註（p.note）、TIP（p.tip）、佔位（.placeholder）；需要卡片時加 css: levels.css |
| quiz | quiz.css | `quiz.js` | 隨堂考：頁面內嵌整個題池，一次顯示 5 題（兩欄三列，第六格計分），開頁隨機抽題，骰子「隨機問題」再抽；點選項即顯示對錯，可顯示全部答案或重做 |
| cover | cover.css | 無 | 封面型：無標題三模組與內容框，主標副標置中（範本 `_template/cover.template.html`），免副標章節號檢查 |
| blank | blank.css | 無 | 空白投影片，內容框留空並畫參考線，排版用；不進翻頁序列 |
| custom | 無 | 無 | 整頁自行維護的互動頁 |

## 7. custom 頁

- 整頁自己維護，可有自己的 CSS 與 JS，可做響應式。
- 必須沿用頁首帶、標題三模組、頁尾署名，並保留標準翻頁連結 `<a class="nav-btn nav-prev" href="…" data-nav="prev">` 與 `nav-next`，產生器只改這兩個 href。
- 頁內自己的步驟切換或控制項放在內容框內，不佔用頁尾按鈕，也不攔截 ← → 翻頁鍵。
- 內容檔只有檔頭，`file:` 指向 HTML；產生器對它只做連結、量測與截圖。

## 8. 待定事項（未討論前不要自行決定）

- 圖片加表格、圖片加文字的混排頁。
- 每一頁的文字內容逐頁討論後才定案。
- 第三章 3.1 考場機率：各考場風況、題型、條件機率的實際資料待作者提供；3.2 考試上電順序、3.4 風向說明為依筆記整理的草稿，待作者確認。

已定案：正文 18 pt 不放大、以放大圖片填版面、法規行固定在 y 642–680（§3）、文加圖版型（§5）、六種元件樣式維持各一種不收斂、圖片集中 `img/`、法規行不顯示標籤且**只能一行**（放不下就只寫法規名稱與條號，條文移進正文；build 檢查）、隨堂考版型與題池機制、隨堂考依章節號自動串接、升力頁頁尾與其他頁一致、第一章與第三章節號表（§4）。

## 9. build.py

- `python build.py`：產生全部頁面、填前後頁連結、量測、截圖，寫 `build/report.md` 與 `build/manifest.json`。
- `python build.py --no-check`：只產生不量測。
- 量測項目：標題框 ≤ 144、內容框 ≤ 421（不含固定在底部的法規行）、表格格子無溢出、文字與圖不與翻頁按鈕重疊、y 642 以下只有法規行 p.tip、前後頁連結目標存在。
- 報告會列出「未納管的 HTML」（根目錄有、pages 沒有），確認後刪除。
- 需要 Edge 或 Chrome；找不到時只產生不量測。
